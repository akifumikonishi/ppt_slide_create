# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a PowerPoint slide creation project. The repository is currently in its initial state with only Claude Code configuration files present.

## Current Structure

- `.claude/` - Claude Code configuration directory
  - `settings.local.json` - Local permissions configuration allowing directory and file operations
  - `commands/create-slide.md.txt` - Custom command file (currently empty)

## Development Setup

Since this is a new project, the development setup will depend on the technology stack chosen:

- For Python-based development: Create `requirements.txt` or `pyproject.toml`
- For Node.js-based development: Create `package.json` with dependencies
- For other languages: Add appropriate dependency management files

## Common Commands

*To be populated once the project technology stack is established*

## Architecture Notes

*To be updated as the codebase develops*

## Project Goals

Based on the repository name, this project appears to be focused on programmatic PowerPoint slide creation. Key considerations for development:

- Choose appropriate libraries for PowerPoint manipulation (e.g., python-pptx for Python, or similar libraries for other languages)
- Consider slide template management
- Plan for content input methods (text, images, data)
- Design for output format handling

## Next Steps for Project Initialization

1. Choose technology stack (Python, Node.js, etc.)
2. Add dependency management files
3. Create initial project structure
4. Add build/test/lint commands
5. Update this CLAUDE.md file with specific guidance